-r requirements.txt

# Empaquetado (PyInstaller): ver PokeFollower.spec (Fase 2 de workbench/windows-packaging/)
pyinstaller==6.22.2

# Generación del .ico multi-resolución (tools/make_icon.py). QImageWriter de Qt no soporta
# escribir múltiples resoluciones en un solo .ico — ver decision.log del ciclo de empaquetado.
Pillow==12.3.0
